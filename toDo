
background changing for loop is currently not working, though set up correctly
    Unsure of issue, resolving

Add active state (click)
    make tip-options change color to hsl(185, 41%, 84%) on click
    if # of people is 0, make :focus outline: red;

add calculator functionality