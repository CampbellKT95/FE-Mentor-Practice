//will filter out all/active/completed using renderedItems

function filterAll() {
    renderListItems();
};

function filterActive() {
    //map through all items with status: false to render
}

function filterComplete() {
    //map through all items with status: true to render
}

